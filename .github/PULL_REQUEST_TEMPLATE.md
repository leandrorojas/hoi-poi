# Pull Request

<!-- PR titles use Conventional Commits prefix where it fits: feat, fix,
     docs, test, chore, refactor. Keep under 70 chars; details go in the
     body, not the title. -->

## Summary

<!-- 1-3 bullets. Lead with the problem this solves, not the diff. -->

-

## Test plan

<!-- Concrete, runnable. CI runs Unit Tests + SonarQube + CodeRabbit +
     Consumer Example Smoke automatically. List anything beyond that here
     (manual smoke runs against the published package, federation
     verification, follow-up tasks). -->

- [ ] `npm test` — all green
- [ ] `npm run lint` — clean
- [ ] `npm run build:lib` — clean (the library build)
